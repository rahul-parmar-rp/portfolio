---
title: Google Sign-In Legacy Placeholder
description: Interactive reconstruction for the empty legacy google-sign-in micro-site file, implemented as the lightweight sign-in flow it was clearly meant to become.
sidebar_position: 7
slug: /google/google-sign-in-legacy
---

import SimpleGoogleAuthDemo from '@site/src/components/google-micro-site-demo/simple-google-auth-demo';

# Google Sign-In Legacy Placeholder

The original `google-sign-in.html` file was empty, but the intended pattern was obvious: a lightweight Google sign-in demo. This page now gives that legacy path a real interactive implementation instead of leaving it as a note about a missing file.

<SimpleGoogleAuthDemo legacy />

## Migration Outcome

- the empty source file now has a live replacement inside Dev Journal
- the canonical minimal flow still lives at `/google/simple-google-auth`
- this page preserves the source-file identity while still behaving like a real demo
